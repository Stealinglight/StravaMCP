---
phase: 01-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/auth/tokenstore.go
  - internal/auth/tokenstore_test.go
  - internal/server/server.go
  - internal/server/server_test.go
  - internal/tools/register.go
  - .gitignore
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-05]

must_haves:
  truths:
    - "`go build` produces a single binary named strava-mcp with zero runtime dependencies"
    - "All logging writes to stderr only -- stdout carries no output except when MCP server is running"
    - "Token store reads and writes JSON atomically using write-then-rename, with 0600 file permissions"
    - "Token store creates ~/.strava/ directory if it does not exist"
    - "Token expiry check uses 5-minute buffer (300 seconds before expires_at)"
    - "Config validates STRAVA_CLIENT_ID and STRAVA_CLIENT_SECRET are set, returns clear error if missing"
    - "MCP server responds to protocol handshake via stdio with empty tool list"
  artifacts:
    - path: "go.mod"
      provides: "Module definition with pinned dependencies"
      contains: "module strava-mcp"
    - path: "main.go"
      provides: "Entry point with subcommand dispatch, slog stderr logging, version flags"
      min_lines: 50
    - path: "internal/config/config.go"
      provides: "Environment variable loading with defaults and validation"
      exports: ["Config", "Load"]
    - path: "internal/auth/tokenstore.go"
      provides: "TokenStore interface and FileTokenStore with atomic writes"
      exports: ["Tokens", "TokenStore", "FileTokenStore", "NewFileTokenStore"]
    - path: "internal/server/server.go"
      provides: "MCP server creation and tool registration orchestration"
      exports: ["New"]
    - path: "internal/tools/register.go"
      provides: "Empty RegisterAll wiring point for Phase 2"
      exports: ["RegisterAll"]
    - path: "internal/auth/tokenstore_test.go"
      provides: "Token store unit tests"
      min_lines: 50
    - path: "internal/config/config_test.go"
      provides: "Config loading tests"
      min_lines: 20
    - path: "internal/server/server_test.go"
      provides: "MCP server creation test"
      min_lines: 20
  key_links:
    - from: "main.go"
      to: "internal/config/config.go"
      via: "config.Load() call"
      pattern: "config\\.Load\\(\\)"
    - from: "main.go"
      to: "internal/auth/tokenstore.go"
      via: "auth.NewFileTokenStore(cfg.TokenPath)"
      pattern: "auth\\.NewFileTokenStore"
    - from: "main.go"
      to: "internal/server/server.go"
      via: "server.New() call"
      pattern: "server\\.New\\("
    - from: "internal/auth/tokenstore.go"
      to: "filesystem"
      via: "atomic write-then-rename"
      pattern: "os\\.Rename\\("
---

<objective>
Scaffold the Go project from scratch, establish the config/token-store foundation, and wire an MCP server shell that responds to protocol handshake.

Purpose: This plan creates all Layer 0 infrastructure (config, token store) plus the MCP server shell. Every subsequent plan depends on these artifacts. The project currently has zero Go code -- this plan creates go.mod, main.go, and the internal package structure.

Output: A buildable Go binary that starts an MCP server (empty tool set) via stdio, with a tested token store and config loader ready for the Strava client and OAuth flow in Plan 02.
</objective>

<execution_context>
@/Users/Stealinglight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Stealinglight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go project scaffold, config loader, and file-based token store</name>
  <files>
    go.mod, go.sum, .gitignore,
    internal/config/config.go, internal/config/config_test.go,
    internal/auth/tokenstore.go, internal/auth/tokenstore_test.go
  </files>
  <read_first>
    src/config/types.ts,
    src/lib/strava-client.ts,
    .planning/phases/01-foundation-and-auth/01-RESEARCH.md,
    .planning/phases/01-foundation-and-auth/01-CONTEXT.md
  </read_first>
  <behavior>
    Config tests:
    - Test 1: Load() returns Config with ClientID/ClientSecret from STRAVA_CLIENT_ID/STRAVA_CLIENT_SECRET env vars
    - Test 2: Load() returns error when STRAVA_CLIENT_ID is empty/unset
    - Test 3: Load() returns error when STRAVA_CLIENT_SECRET is empty/unset
    - Test 4: TokenPath defaults to ~/.strava/tokens.json when STRAVA_TOKEN_PATH is unset
    - Test 5: TokenPath uses STRAVA_TOKEN_PATH value when set

    TokenStore tests:
    - Test 6: Write() creates directory if it does not exist, writes JSON with 0600 permissions
    - Test 7: Write() produces valid JSON with access_token, refresh_token, expires_at fields
    - Test 8: Read() returns Tokens struct from a valid JSON file
    - Test 9: Read() returns error for missing file
    - Test 10: Read() returns error for invalid JSON
    - Test 11: IsExpired() returns true when current time >= expires_at - 300
    - Test 12: IsExpired() returns false when current time < expires_at - 300
    - Test 13: Write() uses atomic write-then-rename (temp file then os.Rename)
    - Test 14: Written file has 0600 permissions (owner read/write only)
  </behavior>
  <action>
    1. Initialize Go module:
       ```
       go mod init strava-mcp
       ```

    2. Add to .gitignore (append to existing):
       ```
       # Go
       strava-mcp
       *.exe
       *.test
       *.out
       /vendor/
       ```

    3. Create `internal/config/config.go`:
       - Package: `config`
       - Struct `Config` with fields:
         - `ClientID string` -- from env `STRAVA_CLIENT_ID`
         - `ClientSecret string` -- from env `STRAVA_CLIENT_SECRET`
         - `TokenPath string` -- from env `STRAVA_TOKEN_PATH`, default `filepath.Join(home, ".strava", "tokens.json")`
         - `Debug bool` -- set programmatically, not from env
       - Function `Load() (*Config, error)`:
         - Read `STRAVA_CLIENT_ID` from os.Getenv -- if empty, return error: `"STRAVA_CLIENT_ID environment variable is required. Get it from https://www.strava.com/settings/api"`
         - Read `STRAVA_CLIENT_SECRET` from os.Getenv -- if empty, return error: `"STRAVA_CLIENT_SECRET environment variable is required. Get it from https://www.strava.com/settings/api"`
         - Read `STRAVA_TOKEN_PATH` from os.Getenv -- if empty, use `filepath.Join(os.UserHomeDir(), ".strava", "tokens.json")`
         - Return populated Config

    4. Create `internal/config/config_test.go`:
       - Use `t.Setenv()` for env var manipulation (auto-cleaned by testing framework)
       - Test all 5 config behaviors listed above

    5. Create `internal/auth/tokenstore.go`:
       - Package: `auth`
       - Struct `Tokens`:
         ```go
         type Tokens struct {
             AccessToken  string `json:"access_token"`
             RefreshToken string `json:"refresh_token"`
             ExpiresAt    int64  `json:"expires_at"`
         }
         ```
         NOTE: Do NOT include client_id or client_secret in the Tokens struct. Per CONTEXT.md, credentials come from env vars only. Token file stores only access_token, refresh_token, expires_at.
       - Interface `TokenStore`:
         ```go
         type TokenStore interface {
             Read() (*Tokens, error)
             Write(tokens *Tokens) error
             IsExpired(tokens *Tokens) bool
         }
         ```
       - Struct `FileTokenStore` with `path string` and `mu sync.RWMutex`
       - Constructor `NewFileTokenStore(path string) *FileTokenStore`
       - `Read()`: RLock, read file, unmarshal JSON, return Tokens
       - `Write()`: Lock, marshal JSON with MarshalIndent (2-space indent), create dir with MkdirAll(dir, 0700), write to `path + ".tmp"` with WriteFile(0600), fsync the temp file, os.Rename(tmp, path)
       - `IsExpired()`: `return time.Now().Unix() >= tokens.ExpiresAt - 300` (5-minute / 300-second buffer)

    6. Create `internal/auth/tokenstore_test.go`:
       - Use t.TempDir() for isolated filesystem tests
       - Test all 9 token store behaviors listed above
       - For atomic write test: verify .tmp file does not remain after successful Write()
       - For permissions test: use os.Stat and check Mode().Perm() == 0600

    7. Fetch dependencies:
       ```
       go get github.com/mark3labs/mcp-go@latest
       go get golang.org/x/sync@latest
       go get github.com/pkg/browser
       go mod tidy
       ```
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && go test ./internal/config/... ./internal/auth/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module strava-mcp`
    - go.mod contains `github.com/mark3labs/mcp-go`
    - go.mod contains `golang.org/x/sync`
    - go.mod contains `github.com/pkg/browser`
    - internal/config/config.go contains `func Load() (*Config, error)`
    - internal/config/config.go contains `STRAVA_CLIENT_ID environment variable is required`
    - internal/config/config.go contains `STRAVA_CLIENT_SECRET environment variable is required`
    - internal/config/config.go contains `STRAVA_TOKEN_PATH`
    - internal/config/config.go contains `.strava` and `tokens.json` (default path)
    - internal/auth/tokenstore.go contains `type Tokens struct`
    - internal/auth/tokenstore.go contains `type TokenStore interface`
    - internal/auth/tokenstore.go contains `type FileTokenStore struct`
    - internal/auth/tokenstore.go contains `os.Rename(` (atomic write)
    - internal/auth/tokenstore.go contains `0600` (file permissions)
    - internal/auth/tokenstore.go contains `ExpiresAt - 300` (5-min buffer)
    - internal/auth/tokenstore.go does NOT contain `ClientID` or `ClientSecret` in Tokens struct
    - internal/auth/tokenstore_test.go exits 0
    - internal/config/config_test.go exits 0
    - .gitignore contains `strava-mcp`
  </acceptance_criteria>
  <done>
    Go module initialized with all dependencies. Config loader reads 3 env vars with validation. FileTokenStore implements atomic write-then-rename with 0600 permissions and 5-min expiry buffer. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP server shell, tool registration wiring, and main.go entry point</name>
  <files>
    main.go,
    internal/server/server.go, internal/server/server_test.go,
    internal/tools/register.go
  </files>
  <read_first>
    internal/config/config.go,
    internal/auth/tokenstore.go,
    .planning/phases/01-foundation-and-auth/01-RESEARCH.md
  </read_first>
  <action>
    1. Create `internal/tools/register.go`:
       - Package: `tools`
       - Import mcp-go server package
       - Function `RegisterAll(s *server.MCPServer)` -- empty body with comment: `// Phase 2 will register Strava tools here`
       - This is the single wiring point the server calls. Phase 2 adds tool registrations here.

    2. Create `internal/server/server.go`:
       - Package: `server`
       - Import `github.com/mark3labs/mcp-go/server` (aliased as `mcpserver` to avoid name collision with the package)
       - Import `strava-mcp/internal/tools`
       - Function `New(version string) *mcpserver.MCPServer`:
         ```go
         func New(version string) *mcpserver.MCPServer {
             s := mcpserver.NewMCPServer(
                 "strava-mcp",
                 version,
                 mcpserver.WithLogging(),
             )
             tools.RegisterAll(s)
             return s
         }
         ```
       - NOTE: The server.New function takes version string only. The StravaClient dependency will be added in Plan 02 when tools.RegisterAll needs it.

    3. Create `internal/server/server_test.go`:
       - Package: `server_test`
       - Test `TestNewCreatesServer`: call server.New("test"), assert result is not nil
       - Test `TestServerHandshake` (if feasible with mcp-go API): verify the server can be created and has the name "strava-mcp"

    4. Create `main.go` at project root:
       - Package: `main`
       - Build-time variables (set via ldflags):
         ```go
         var (
             Version = "dev"
             Commit  = "none"
             Date    = "unknown"
         )
         ```
       - FIRST LINES of main():
         ```go
         // Safety net: redirect standard log and pkg/browser to stderr
         log.SetOutput(os.Stderr)
         browser.Stdout = os.Stderr
         browser.Stderr = os.Stderr
         ```
       - Parse args from os.Args[1:] manually (no CLI framework):
         - `--version` flag: print `"strava-mcp %s (%s) built %s\n"` with Version, Commit, Date to STDERR, then os.Exit(0)
         - `--debug` flag: sets debug bool to true
         - `auth` subcommand: call `runAuth()` placeholder that prints "Auth flow not yet implemented" to stderr and exits
         - Default (no subcommand): run MCP server
       - Configure slog:
         ```go
         level := slog.LevelInfo
         if debug {
             level = slog.LevelDebug
         }
         logger := slog.New(slog.NewTextHandler(os.Stderr, &slog.HandlerOptions{Level: level}))
         slog.SetDefault(logger)
         ```
       - runServer function:
         ```go
         func runServer(debug bool) {
             s := server.New(Version)
             slog.Info("starting MCP server", "name", "strava-mcp", "version", Version)
             if err := mcpserver.ServeStdio(s); err != nil {
                 slog.Error("server error", "err", err)
                 os.Exit(1)
             }
         }
         ```
       - runAuth placeholder:
         ```go
         func runAuth() {
             fmt.Fprintln(os.Stderr, "Auth flow not yet implemented. See Plan 02.")
             os.Exit(1)
         }
         ```
       - CRITICAL: main.go must NEVER use fmt.Print/fmt.Println to stdout. All user-facing output uses fmt.Fprint(os.Stderr, ...). The only stdout writer is mcp-go ServeStdio.

    5. Run `go build -o strava-mcp .` to verify the binary compiles.

    6. Run `go vet ./...` to catch issues.
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && go build -o strava-mcp . && go test ./internal/server/... -count=1 -v && go vet ./... && go test ./... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - main.go contains `var Version = "dev"`
    - main.go contains `var Commit = "none"`
    - main.go contains `var Date = "unknown"`
    - main.go contains `log.SetOutput(os.Stderr)` (safety net)
    - main.go contains `browser.Stdout = os.Stderr` (pkg/browser redirect)
    - main.go contains `browser.Stderr = os.Stderr`
    - main.go contains `slog.NewTextHandler(os.Stderr` (structured logging to stderr)
    - main.go contains `"--version"` flag handling
    - main.go contains `"--debug"` flag handling
    - main.go contains `"auth"` subcommand handling
    - main.go contains `server.New(Version)`
    - main.go contains `mcpserver.ServeStdio` or `server.ServeStdio`
    - main.go does NOT contain `fmt.Println(` (no stdout writes)
    - internal/server/server.go contains `func New(`
    - internal/server/server.go contains `mcpserver.NewMCPServer(`
    - internal/server/server.go contains `"strava-mcp"` (server name)
    - internal/server/server.go contains `tools.RegisterAll(`
    - internal/tools/register.go contains `func RegisterAll(`
    - internal/tools/register.go contains `Phase 2` comment
    - `go build -o strava-mcp .` exits 0
    - `go vet ./...` exits 0
    - `go test ./... -count=1 -race` exits 0
  </acceptance_criteria>
  <done>
    main.go compiles to a single binary. All stderr logging is correctly configured. MCP server shell starts via ServeStdio with empty tool list. Version/debug/auth flags all dispatch correctly. Full test suite passes with -race.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `go build -o strava-mcp .` produces binary (INFRA-01)
2. `go test ./... -count=1 -race` all tests pass
3. `go vet ./...` clean
4. Binary version check: `./strava-mcp --version 2>&1` prints version info to stderr
5. Token store: tests confirm atomic write, permissions, expiry buffer (INFRA-02)
6. MCP server: server.New creates server with "strava-mcp" name (INFRA-05 partial -- full stdio test in Plan 02)
</verification>

<success_criteria>
- Go project compiles to a single binary with `go build`
- Config loader validates required env vars with actionable error messages
- FileTokenStore implements atomic write-then-rename with 0600 permissions
- MCP server shell created with mcp-go, empty tool list, ready for tool registration
- main.go dispatches auth/server/version subcommands
- All output goes to stderr -- stdout is reserved for MCP JSON-RPC
- All tests pass including race detector
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md`
</output>
