## Summary

<!-- What does this PR do? Link any related issues with "Fixes #123" or "Closes #123". -->

## Changes

- 

## Checklist

- [ ] `go test ./...` passes
- [ ] `go vet ./...` clean
- [ ] New tool? Added handler, registered in `register.go`, added tests
- [ ] Follows conventional commit format (`feat:`, `fix:`, `docs:`, `refactor:`)
- [ ] All logging to stderr via `slog` (stdout is MCP JSON-RPC)
- [ ] No secrets or tokens in the diff
