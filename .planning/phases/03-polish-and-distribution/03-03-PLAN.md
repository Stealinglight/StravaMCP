---
phase: 03-polish-and-distribution
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - README.md
  - docs/_config.yml
  - docs/index.md
autonomous: false
requirements:
  - DOCS-01
must_haves:
  truths:
    - "README describes the Go binary project, not the TypeScript/Lambda project"
    - "README has badge row with Go version, Go Report Card, Release, License, Build Status badges"
    - "README has two Mermaid diagrams: high-level flow and internal architecture"
    - "README has tool reference table grouped by category with one-line descriptions"
    - "README has Quick Start with two installation paths: go install and binary download"
    - "README has collapsible sections for detailed content"
    - "README communicates zero cloud infrastructure as key differentiator"
    - "docs/ site describes the Go binary, not Lambda/TypeScript"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-quality project README"
      min_lines: 150
      contains: "strava-mcp"
    - path: "docs/_config.yml"
      provides: "Updated Jekyll config for Go project"
      contains: "Go binary"
    - path: "docs/index.md"
      provides: "Simplified Go-focused landing page"
      contains: "go install"
  key_links:
    - from: "README.md"
      to: ".goreleaser.yaml"
      via: "Installation instructions reference GitHub Releases"
      pattern: "github.com/Stealinglight/StravaMCP/releases"
    - from: "README.md"
      to: "internal/tools/"
      via: "Tool reference table lists all 11 tools"
      pattern: "get_activities"
    - from: "README.md"
      to: "go.mod"
      via: "go install command uses module path"
      pattern: "go install github.com/Stealinglight/StravaMCP"
---

<objective>
Write a portfolio-quality README from scratch and rewrite the docs/ site for the Go binary project.

Purpose: The README is the primary user-facing artifact and portfolio showcase (D-01 through D-07). The docs/ site must be updated to stop describing the Lambda/TypeScript project (D-13). Together these make the project presentable to both users and potential employers.
Output: Complete README.md with premium visual polish; simplified docs/ site for the Go binary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-and-distribution/03-CONTEXT.md
@.planning/phases/03-polish-and-distribution/03-RESEARCH.md

@internal/tools/register.go
@internal/tools/activities.go
@internal/tools/athlete.go
@internal/tools/streams.go
@internal/tools/clubs.go
@internal/tools/uploads.go
@internal/config/config.go
@internal/auth/oauth.go

<interfaces>
<!-- From internal/tools/register.go - all 11 tools -->
// RegisterAll registers all MCP tools with the server.
// - Activities: get_activities, get_activity_by_id, create_activity, update_activity, get_activity_zones
// - Athlete: get_athlete, get_athlete_stats
// - Streams: get_activity_streams
// - Clubs: get_club_activities
// - Uploads: create_upload, get_upload

<!-- From internal/config/config.go - env vars -->
// STRAVA_CLIENT_ID (required)
// STRAVA_CLIENT_SECRET (required)
// STRAVA_TOKEN_PATH (optional, default: ~/.strava/tokens.json)

<!-- From main.go - CLI interface -->
// strava-mcp auth     (runs OAuth browser flow)
// strava-mcp --version (prints version)
// strava-mcp --debug   (debug logging)
// strava-mcp           (default: runs MCP server on stdio)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write portfolio-quality README.md from scratch</name>
  <files>README.md</files>
  <read_first>README.md, internal/tools/register.go, internal/tools/activities.go, internal/tools/athlete.go, internal/tools/streams.go, internal/tools/clubs.go, internal/tools/uploads.go, internal/config/config.go, internal/auth/oauth.go, main.go, go.mod</read_first>
  <action>
Completely replace README.md with a new file per D-01 (start fresh, old content is 100% TypeScript/Lambda). The README must include ALL of the following sections in this order. This is a portfolio piece, visual polish matters.

SECTION 1 - Badge row (D-04): Place at the very top, before the title. Five badges on one line:

[![Go Version](https://img.shields.io/github/go-mod/go-version/Stealinglight/StravaMCP)](https://go.dev/)
[![Go Report Card](https://goreportcard.com/badge/github.com/Stealinglight/StravaMCP)](https://goreportcard.com/report/github.com/Stealinglight/StravaMCP)
[![Release](https://img.shields.io/github/v/release/Stealinglight/StravaMCP)](https://github.com/Stealinglight/StravaMCP/releases/latest)
[![Build Status](https://img.shields.io/github/actions/workflow/status/Stealinglight/StravaMCP/release.yml)](https://github.com/Stealinglight/StravaMCP/actions)
[![License](https://img.shields.io/github/license/Stealinglight/StravaMCP)](LICENSE)

SECTION 2 - Title and hero section (D-02): Communicate the key differentiator per CONTEXT.md specifics:

# StravaMCP

**A fast, self-contained Go binary that gives any MCP client full access to the Strava API, zero cloud infrastructure required.**

Follow with a 3-4 line paragraph explaining: single binary, stdio MCP transport, works with Claude Desktop/Cursor/any MCP client, automatic OAuth browser flow, file-based token storage. Emphasize "no AWS, no Docker, no database."

SECTION 3 - High-level architecture diagram (D-06, D-07): Mermaid flow diagram in a fenced code block with language "mermaid". Show: MCP Client (Claude, Cursor, etc.) connected via stdio to strava-mcp Go binary, which connects via HTTPS to Strava API v3, and reads/writes ~/.strava/tokens.json.

SECTION 4 - Features section (D-02): Bullet list with descriptive icons/emoji. Include these features exactly:
- Single binary, no runtime dependencies, no Docker, no cloud
- 11 MCP tools covering activities, athlete stats, streams, clubs, and uploads
- Automatic OAuth browser flow, one command to authenticate
- Transparent token refresh with concurrent request coalescing
- Cross-platform, macOS (Intel + Apple Silicon) and Linux (amd64 + arm64)
- Install via go install, Homebrew, or direct binary download

SECTION 5 - Quick Start section (D-08): Three installation options presented as equal:

Option A: go install github.com/Stealinglight/StravaMCP@latest
Option B: brew install Stealinglight/tap/strava-mcp
Option C: Download binary from GitHub Releases (link to https://github.com/Stealinglight/StravaMCP/releases/latest)

Then show the setup steps:
1. Create a Strava API application at https://www.strava.com/settings/api
2. Set env vars: export STRAVA_CLIENT_ID=your_id and export STRAVA_CLIENT_SECRET=your_secret
3. Authenticate: strava-mcp auth (opens browser, completes OAuth, saves tokens)
4. Configure your MCP client. Show Claude Desktop config JSON example with mcpServers block containing strava server with command "strava-mcp" and env vars STRAVA_CLIENT_ID and STRAVA_CLIENT_SECRET.

Include macOS Gatekeeper note (Pitfall 5): For direct binary downloads on macOS, show xattr -d com.apple.quarantine strava-mcp command.

SECTION 6 - Tool Reference section (D-05): Grouped table with one-line descriptions per category. Use actual tool names from register.go. Wrap in a collapsible details/summary HTML block titled "Tool Reference (11 tools)":

| Tool | Category | Description |
|------|----------|-------------|
| get_activities | Activities | List recent activities with date filtering and pagination |
| get_activity_by_id | Activities | Get detailed activity info including laps, splits, and segment efforts |
| create_activity | Activities | Create a new manual activity |
| update_activity | Activities | Update an existing activity (name, description, sport type, gear) |
| get_activity_zones | Activities | Get heart rate and power zone distribution |
| get_athlete | Athlete | Get authenticated athlete profile |
| get_athlete_stats | Athlete | Get aggregate statistics (recent/YTD/all-time totals) |
| get_activity_streams | Streams | Get time-series telemetry data (HR, GPS, power, cadence, altitude) |
| get_club_activities | Clubs | List recent activities from club members |
| create_upload | Uploads | Upload activity files (GPX, TCX, FIT) |
| get_upload | Uploads | Check upload processing status |

SECTION 7 - Architecture section (D-07): Detailed internal Mermaid diagram inside a collapsible details/summary block titled "Architecture". Show: main.go -> subcommand choice -> auth leads to OAuth Browser Flow -> Token Store; default leads to MCP Server -> Tool Handlers (11 tools) -> Strava Client -> Auto Token Refresh + singleflight -> Token Store; Strava Client -> Strava API v3. Below diagram list key design decisions: stderr-only logging via slog, singleflight.Group for concurrent refreshes, atomic write-then-rename token store, static binary with zero CGO.

SECTION 8 - Configuration section: Table of environment variables:
| Variable | Required | Default | Description |
| STRAVA_CLIENT_ID | Yes | (none) | Strava API application client ID |
| STRAVA_CLIENT_SECRET | Yes | (none) | Strava API application client secret |
| STRAVA_TOKEN_PATH | No | ~/.strava/tokens.json | Path to token storage file |

SECTION 9 - Development section: Brief build/test commands (go build ., go test ./...) with link to CONTRIBUTING.md.

SECTION 10 - License section: "ISC" with link to LICENSE file.

SECTION 11 - Links section: Strava API docs, MCP specification, GitHub releases.

IMPORTANT: Do NOT include any references to TypeScript, Bun, Lambda, AWS, DynamoDB, Express, or any old-project technology. Do NOT include emoji in headings (D-02 says emoji in feature highlights, not headings). The README should read as if the Go project was always the only project.
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && grep -c 'img.shields.io' README.md && grep -c 'mermaid' README.md && grep -c 'get_activities' README.md && grep -c 'go install github.com/Stealinglight/StravaMCP' README.md && grep -c 'brew install' README.md && grep -c 'STRAVA_CLIENT_ID' README.md && test ! "$(grep -ci 'lambda\|dynamodb\|bun\|typescript\|aws sam' README.md)" -gt 0 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - README.md has 5+ badge URLs from img.shields.io or goreportcard.com
    - README.md contains two mermaid fenced code blocks (high-level flow and internal architecture)
    - README.md contains all 11 tool names: get_activities, get_activity_by_id, create_activity, update_activity, get_activity_zones, get_athlete, get_athlete_stats, get_activity_streams, get_club_activities, create_upload, get_upload
    - README.md contains "go install github.com/Stealinglight/StravaMCP@latest"
    - README.md contains "brew install Stealinglight/tap/strava-mcp"
    - README.md contains "strava-mcp auth"
    - README.md contains STRAVA_CLIENT_ID and STRAVA_CLIENT_SECRET
    - README.md contains "mcpServers" (Claude Desktop config example)
    - README.md contains a details HTML element (collapsible sections)
    - README.md contains "xattr -d com.apple.quarantine" (macOS Gatekeeper note)
    - README.md does NOT contain Lambda, DynamoDB, Bun, TypeScript, AWS SAM, bun install, sam deploy, Express
    - README.md is at least 150 lines long
    - README.md first line contains a badge (starts with "[![")
  </acceptance_criteria>
  <done>README.md is a complete portfolio-quality document with badges, Mermaid diagrams, tool reference, dual installation paths, MCP client config, and zero legacy references</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/ site for Go binary</name>
  <files>docs/_config.yml, docs/index.md</files>
  <read_first>docs/_config.yml, docs/index.md</read_first>
  <action>
Rewrite the docs/ site per D-13. Replace Lambda content with Go-focused pages. Keep the GitHub Pages URL alive. Delete all Lambda-specific doc pages that are no longer relevant.

STEP 1 - Delete old Lambda doc pages. Delete these files from docs/ (they describe TypeScript/Lambda/AWS deployment that no longer exists):
- docs/api.md
- docs/authentication.md
- docs/changelog.md
- docs/coaching-guide.md
- docs/deployment.md
- docs/development.md
- docs/examples.md
- docs/freetier.md
- docs/plans/ (entire directory if present)
- docs/_includes/ (directory if present)

Keep only: docs/_config.yml, docs/index.md

STEP 2 - Update docs/_config.yml. Change description to "MCP server for Strava API - a fast Go binary with zero cloud infrastructure". Change footer_content to "Built with Go | (link to GitHub)". Remove the collections: block (no longer needed with single page). Keep all other settings (remote_theme, color_scheme: dark, search, aux_links, callouts, enable_copy_code_button).

STEP 3 - Rewrite docs/index.md. Create a simple Go-focused landing page with Jekyll front matter (layout: default, title: Home, nav_order: 1). Content should include:
- Hero: "StravaMCP" heading with tagline about fast Go binary, zero cloud infrastructure
- Buttons: "Get Started" linking to #quick-start, "View on GitHub" linking to repo
- "What is this?" section explaining MCP server for AI assistants, single Go binary, no cloud/Docker/database
- Key Features bullet list (same as README features)
- Quick Start section with three install options (go install, brew, binary download)
- Configure section: Strava API app creation, env var setup, strava-mcp auth command
- Connect to Claude Desktop section: JSON config example with mcpServers block
- Available Tools summary table: 5 categories with tool counts and brief descriptions
- Links section: GitHub repo, Strava API docs, MCP spec
- Footer line: "Built with Go by Stealinglight"

IMPORTANT: The docs/index.md must NOT contain any references to Lambda, AWS, Bun, TypeScript, DynamoDB, SAM, or any old-project technology.
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && grep -c 'Go binary' docs/_config.yml && grep -c 'go install' docs/index.md && test ! -f docs/deployment.md && test ! -f docs/freetier.md && test ! -f docs/api.md && test ! "$(grep -ci 'lambda\|aws\|bun\|typescript\|dynamodb' docs/index.md)" -gt 0 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - docs/_config.yml description contains "Go binary"
    - docs/_config.yml footer contains "Built with Go"
    - docs/_config.yml does NOT contain "Lambda" or "AWS" or "Free Tier"
    - docs/_config.yml does NOT have collections: block
    - docs/index.md contains "go install github.com/Stealinglight/StravaMCP@latest"
    - docs/index.md contains "brew install Stealinglight/tap/strava-mcp"
    - docs/index.md contains "strava-mcp auth"
    - docs/index.md contains STRAVA_CLIENT_ID
    - docs/index.md does NOT contain Lambda, AWS, Bun, TypeScript, DynamoDB, SAM
    - docs/deployment.md does NOT exist
    - docs/freetier.md does NOT exist
    - docs/api.md does NOT exist
    - docs/authentication.md does NOT exist
    - docs/coaching-guide.md does NOT exist
    - docs/examples.md does NOT exist
    - docs/development.md does NOT exist
    - docs/changelog.md does NOT exist
  </acceptance_criteria>
  <done>docs/ site updated for Go binary with all Lambda content removed; GitHub Pages URL remains functional</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of README on GitHub</name>
  <files>README.md, docs/index.md</files>
  <action>Push changes to a branch and verify visual rendering on GitHub.</action>
  <what-built>Complete README.md rewrite with badges, Mermaid diagrams, tool reference table, collapsible sections, and Quick Start guide. Also docs/ site rewrite.</what-built>
  <how-to-verify>
    1. Open README.md in a markdown previewer or push to a branch and view on GitHub
    2. Verify badges render correctly (5 badges in a row at the top)
    3. Verify both Mermaid diagrams render (high-level flow and internal architecture)
    4. Verify the tool reference table is inside a collapsible section and lists all 11 tools
    5. Verify Quick Start shows go install, Homebrew, and binary download paths
    6. Verify Claude Desktop config JSON is correct and copyable
    7. Verify no TypeScript/Lambda/AWS references remain anywhere
    8. Verify the overall visual impression is "portfolio-quality"
    9. Optionally: check docs/index.md renders correctly with just-the-docs theme
  </how-to-verify>
  <verify>Human visual inspection confirms README renders correctly on GitHub</verify>
  <done>Visual quality approved by human reviewer</done>
  <resume-signal>Type "approved" or describe any visual/content issues to fix</resume-signal>
</task>

</tasks>

<verification>
- README.md has badges, Mermaid diagrams, tool reference, Quick Start, and configuration sections
- README.md has zero references to TypeScript, Lambda, AWS, Bun, DynamoDB, or Express
- README.md lists all 11 tools from register.go
- docs/ site has been simplified to a single Go-focused landing page
- All old Lambda-specific doc pages have been deleted
- Human has verified visual quality of the rendered README
</verification>

<success_criteria>
- README is a portfolio-quality document that a new user can follow to go from zero to working MCP server
- README accurately describes the Go binary project with all current tools and configuration
- docs/ site loads on GitHub Pages with Go-focused content
- Visual verification confirms badges render, Mermaid diagrams display, and collapsible sections work
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-and-distribution/03-03-SUMMARY.md`
</output>
