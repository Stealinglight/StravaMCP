---
phase: 05-openclaw-positioning-performance-messaging
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - docs/index.md
  - docs/integration.md
  - docs/_config.yml
autonomous: false
requirements:
  - MSG-03
  - MSG-04

must_haves:
  truths:
    - "docs/index.md hero tagline matches README hero tagline"
    - "docs/index.md contains Why Go? performance table identical to README"
    - "docs/integration.md exists with OpenClaw/ZeroClaw integration guide"
    - "docs/integration.md contains the same ecosystem Mermaid diagram as README"
    - "docs/_config.yml description says 'Production-grade MCP server for Strava API'"
    - "docs/integration.md appears in site navigation via nav_order frontmatter"
    - "No banned words in any docs file"
  artifacts:
    - path: "docs/index.md"
      provides: "Updated docs home page with production-grade positioning and performance content"
      contains: "## Why Go?"
    - path: "docs/integration.md"
      provides: "New OpenClaw/ZeroClaw integration guide page"
      contains: "## What is OpenClaw/ZeroClaw?"
    - path: "docs/_config.yml"
      provides: "Updated site description"
      contains: "Production-grade MCP server"
  key_links:
    - from: "docs/index.md"
      to: "docs/integration.md"
      via: "markdown link in Links section"
      pattern: "integration"
    - from: "docs/integration.md"
      to: "Mermaid ecosystem diagram"
      via: "graph TD block identical to README"
      pattern: "graph TD"
    - from: "docs/_config.yml"
      to: "Site description"
      via: "description field"
      pattern: "Production-grade"
---

<objective>
Update docs site to mirror README production-grade positioning and create the OpenClaw/ZeroClaw integration guide page.

Purpose: The docs site must stand on its own (D-08) -- readers who land on the docs site directly should get the same production-grade messaging as the README. This plan updates docs/index.md with matching tagline and features, creates the new docs/integration.md with full integration guide, and updates docs/_config.yml description. It also includes a checkpoint for the manual GitHub "About" description update (D-11).

Output: Updated docs/index.md, new docs/integration.md, updated docs/_config.yml, and user notification for manual GitHub About update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-openclaw-positioning-performance-messaging/05-CONTEXT.md
@.planning/phases/05-openclaw-positioning-performance-messaging/05-RESEARCH.md
@.planning/phases/05-openclaw-positioning-performance-messaging/05-UI-SPEC.md
@.planning/phases/05-openclaw-positioning-performance-messaging/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 output: Updated README.md with new sections -->
<!-- Executor MUST read README.md to get exact tagline, features, performance table, and ecosystem diagram -->
<!-- These must be mirrored/adapted for docs site per the Content Overlap Matrix in UI-SPEC -->

From README.md (after Plan 01):
- Hero tagline: "A production-grade MCP server that gives agent frameworks full access to the Strava API -- single Go binary, zero infrastructure."
- Features: 7 bullets with reliability emphasis (singleflight, atomic writes, zero-CGO)
- Why Go? table: 5-row comparison (startup, memory, binary, deps, runtime)
- Agent Framework Integration: Mermaid graph TD ecosystem diagram
- JSON config: mcpServers.strava pattern

From docs/index.md (current):
- Frontmatter: layout: default, title: Home, nav_order: 1
- Typography: {: .fs-9 } for title, {: .fs-6 .fw-300 } for subtitle
- CTA buttons: {: .btn .btn-primary .fs-5 .mb-4 .mb-md-0 .mr-2 } pattern

From docs/_config.yml (current):
- description: "MCP server for Strava API - a fast Go binary with zero cloud infrastructure"
- Only the description field changes; all other fields remain untouched
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/index.md with production-grade positioning and performance content</name>
  <files>docs/index.md</files>
  <read_first>
    - README.md (Plan 01 output -- source of truth for tagline, features, performance table)
    - docs/index.md (current full content to understand structure)
    - .planning/phases/05-openclaw-positioning-performance-messaging/05-UI-SPEC.md (docs/index.md structure contract, content overlap matrix, typography rules)
  </read_first>
  <action>
Rewrite docs/index.md following the Content Structure Contract from UI-SPEC.md. Keep the same frontmatter and just-the-docs typography patterns. Preserve the Quick Start, Available Tools, and bottom sections. Update the top sections and insert new content.

**1. Frontmatter:** Keep existing YAML frontmatter unchanged (layout: default, title: Home, nav_order: 1).

**2. Title and tagline (D-09 -- match README):**

The `# StravaMCP` heading with `{: .fs-9 }` stays. Replace the tagline text (the `{: .fs-6 .fw-300 }` paragraph) with:

`A production-grade MCP server that gives agent frameworks full access to the Strava API -- single Go binary, zero infrastructure.`

Keep the two CTA buttons and horizontal rule after them unchanged.

**3. What is this? section (reframe for production-grade, D-09):**

Replace the paragraph under `## What is this?` with:

`**StravaMCP** is a [Model Context Protocol](https://modelcontextprotocol.io) server built in Go for the [OpenClaw/ZeroClaw](https://github.com/Stealinglight) agent framework ecosystem. It connects AI agents to the Strava API through a single static binary on your machine. Communicates over stdio, works with any MCP-compatible client, handles OAuth authentication through an automatic browser flow, and stores tokens locally. No cloud services, no containers, no runtime dependencies.`

**4. Key Features (updated to match README, D-09 reliability emphasis):**

Replace the `### Key Features` bullet list with these 7 bullets:

- **Single binary, zero runtime dependencies** -- no Docker, no cloud, no database
- **11 MCP tools** covering activities, athlete stats, streams, clubs, and uploads
- **Automatic OAuth browser flow** -- one command to authenticate
- **Concurrent token refresh via singleflight** -- no thundering herd on expired tokens
- **Atomic write-then-rename token store** -- crash-safe credential persistence
- **Zero-CGO static binary** -- no C library dependencies, runs anywhere
- **Cross-platform** -- macOS (Intel + Apple Silicon) and Linux (amd64 + arm64)

**5. NEW: Why Go? section (D-04, D-05, D-06, D-08 -- docs stands alone):**

Insert a new `## Why Go?` section AFTER Key Features, BEFORE Quick Start. Copy the exact same intro sentence and table from README.md:

Intro: `StravaMCP is written in Go for the same reason the RustyClaw ecosystem exists: **performance and simplicity matter for tool servers that agents call hundreds of times per session.**`

Table columns: (empty) | Go (StravaMCP) | Python | Node.js
Row 1: **Startup time** | ~10ms | ~500ms | ~200ms
Row 2: **Memory footprint** | ~8MB | ~30MB | ~40MB
Row 3: **Binary size** | 7MB (single file) | ~50MB+ (runtime + deps) | ~60MB+ (runtime + node_modules)
Row 4: **Dependencies** | 3 direct | Varies (pip) | Varies (npm)
Row 5: **Runtime required** | None | Python interpreter | Node.js runtime

Disclaimer (italic, immediately below table): `*Estimates based on known Go/Python/Node.js runtime characteristics for comparable MCP servers. Not formal benchmarks.*`

**6. Quick Start section -- keep existing content unchanged.**

**7. Available Tools section -- keep existing content unchanged.**

**8. Links section -- add integration page link as first item:**

Add `[Agent Framework Integration](integration) -- wire StravaMCP into OpenClaw/ZeroClaw` as the first bullet in the Links list, before the GitHub Repository link.

**9. Footer line -- keep unchanged.**

**10. Tone sweep:** Verify zero occurrences of: portfolio, showcase, demo, toy, experiment, hobby, personal, side project.
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && grep -c "## Why Go?" docs/index.md && grep -c "production-grade" docs/index.md && grep -ciE "portfolio|showcase|demo|toy|experiment|hobby|personal project|side project" docs/index.md | grep -q "^0$" && echo "TONE_CLEAN" || echo "TONE_DIRTY"</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md frontmatter contains `layout: default`, `title: Home`, `nav_order: 1`
    - docs/index.md contains `{: .fs-9 }` after the `# StravaMCP` heading
    - docs/index.md tagline contains `production-grade MCP server that gives agent frameworks full access`
    - docs/index.md contains `{: .fs-6 .fw-300 }` after the tagline
    - docs/index.md contains exactly the string `## Why Go?` (one occurrence)
    - docs/index.md contains the performance table with `Go (StravaMCP)` column header
    - docs/index.md contains `*Estimates based on known Go/Python/Node.js runtime characteristics` (D-06 disclaimer)
    - docs/index.md contains `singleflight` in Key Features
    - docs/index.md contains `Atomic write-then-rename` in Key Features
    - docs/index.md contains `Zero-CGO` in Key Features
    - docs/index.md contains `OpenClaw/ZeroClaw` in What is this? section
    - docs/index.md Links section contains `[Agent Framework Integration](integration)`
    - docs/index.md contains zero occurrences of banned words (portfolio, showcase, demo, toy, experiment, hobby, side project)
    - Performance table numbers match README exactly: ~10ms, ~500ms, ~200ms, ~8MB, ~30MB, ~40MB, 7MB, ~50MB+, ~60MB+
  </acceptance_criteria>
  <done>docs/index.md mirrors README production-grade positioning with matching tagline, updated feature bullets, full Why Go? performance table, and link to the new integration page.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/integration.md and update docs/_config.yml</name>
  <files>docs/integration.md, docs/_config.yml</files>
  <read_first>
    - README.md (Plan 01 output -- source of truth for ecosystem diagram and JSON config)
    - docs/index.md (Task 1 output -- verify consistent messaging)
    - docs/_config.yml (current config -- only description field changes)
    - .planning/phases/05-openclaw-positioning-performance-messaging/05-UI-SPEC.md (docs/integration.md structure contract, frontmatter pattern, typography rules)
    - .planning/phases/05-openclaw-positioning-performance-messaging/05-RESEARCH.md (reliability feature descriptions, JSON config snippet, ecosystem diagram)
  </read_first>
  <action>
**Part A: Create docs/integration.md (D-07)**

Create a new file `docs/integration.md` with the full OpenClaw/ZeroClaw integration guide. The file must have YAML frontmatter with these three fields: `layout: default`, `title: Agent Framework Integration`, `nav_order: 2`.

After the frontmatter, the page structure must be:

SECTION 1 - Page header:
- `# Agent Framework Integration` with `{: .fs-9 }` class
- Subtitle: `Wire StravaMCP into OpenClaw/ZeroClaw agent frameworks as a stdio tool provider.` with `{: .fs-6 .fw-300 }` class
- Two CTA buttons: "Quick Start" linking to `/#quick-start` (primary button), "View on GitHub" linking to `https://github.com/Stealinglight/StravaMCP` (secondary button)
- Horizontal rule separator

SECTION 2 - `## What is OpenClaw/ZeroClaw?`
Two paragraphs:

Paragraph 1: `OpenClaw and ZeroClaw are agent framework ecosystems that orchestrate AI agents with access to external services. Each service is exposed as an MCP (Model Context Protocol) server that communicates over stdio transport -- the agent framework launches each server as a subprocess and routes tool calls through the standard MCP protocol.`

Paragraph 2: `StravaMCP is one of several high-performance MCP servers in this ecosystem. Unlike typical Python or JavaScript MCP servers that require interpreter runtimes and large dependency trees, the RustyClaw ecosystem favors compiled languages (Go, Rust) for sub-second startup, minimal memory footprint, and zero-dependency deployment.`

SECTION 3 - `## Ecosystem Architecture`
Mermaid diagram (graph TD) -- MUST be identical to the one in README.md:
- Node AF: `Agent Framework<br/>(OpenClaw / ZeroClaw)`
- Node SM: `StravaMCP<br/>Go &middot; 7MB` (connected from AF via `stdio`)
- Node SK: `SlackMCP<br/>Rust` (connected from AF via `stdio`)
- Node WR: `WebResearchMCP<br/>Rust` (connected from AF via `stdio`)
- Node VM: `VideoMCP<br/>Rust` (connected from AF via `stdio`)
- Node SA: `Strava API v3` (connected from SM via `HTTPS`)
- Node SKA: `Slack API` (connected from SK via `HTTPS`)
- Node WRA: `Web Search APIs` (connected from WR via `HTTPS`)
- Node VMA: `Video APIs` (connected from VM via `HTTPS`)

After the diagram, a bullet list of MCP server characteristics:
- Starts in milliseconds (no interpreter warm-up)
- Uses minimal memory (single-digit MB at idle)
- Communicates over stdio (no HTTP server, no port conflicts)
- Handles its own authentication and API access

SECTION 4 - `## Wiring Configuration`
Intro text: `Add StravaMCP to your agent framework's MCP server configuration. The agent framework launches the binary as a subprocess and communicates over stdin/stdout:`

JSON config block (same canonical format as README):
`{"mcpServers":{"strava":{"command":"strava-mcp","env":{"STRAVA_CLIENT_ID":"your_client_id","STRAVA_CLIENT_SECRET":"your_client_secret"}}}}`
(Format as pretty-printed JSON with 2-space indentation in the actual file)

Subsection `### Environment Variables` with a 3-row table:
- STRAVA_CLIENT_ID | Yes | Your Strava API application client ID
- STRAVA_CLIENT_SECRET | Yes | Your Strava API application client secret
- STRAVA_TOKEN_PATH | No | Custom path to token storage (default: ~/.strava/tokens.json)

Subsection `### First-Time Setup` with instructions:
- Export STRAVA_CLIENT_ID and STRAVA_CLIENT_SECRET
- Run `strava-mcp auth` (opens browser)
- Note that after authentication, tokens are stored locally and refreshed automatically

SECTION 5 - `## Example Workflows`
Three workflow examples using actual StravaMCP tool names:

Workflow 1: Training Log Analysis -- uses strava_get_activities, strava_get_activity_streams, strava_get_athlete_stats
Workflow 2: Activity Journaling -- uses strava_get_activities, strava_get_activity_by_id, strava_update_activity, strava_get_activity_zones
Workflow 3: Club Activity Monitoring -- uses strava_get_club_activities

SECTION 6 - `## Performance Characteristics`
Brief summary with 4 bullets:
- Sub-second startup (~10ms)
- Minimal memory (~8MB)
- Small binary (7MB)
- Zero runtime -- no Python interpreter, no Node.js runtime, no virtual environment

Link to full comparison: `For the full performance comparison, see [Why Go?](/#why-go) on the home page.`

**Part B: Update docs/_config.yml description (D-10)**

In `docs/_config.yml`, change ONLY the `description` field value from:
`MCP server for Strava API - a fast Go binary with zero cloud infrastructure`
to:
`Production-grade MCP server for Strava API -- built in Go for agent frameworks`

Do NOT modify any other field in _config.yml. All other values (title, url, baseurl, remote_theme, footer_content, color_scheme, search, aux_links, callouts, enable_copy_code_button, nav_sort) remain exactly as they are.
  </action>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && test -f docs/integration.md && echo "FILE_EXISTS" && grep -c "nav_order: 2" docs/integration.md && grep -c "## What is OpenClaw/ZeroClaw?" docs/integration.md && grep -c "graph TD" docs/integration.md && grep -c "Production-grade" docs/_config.yml && grep -ciE "portfolio|showcase|demo|toy|experiment|hobby|personal project|side project" docs/integration.md docs/_config.yml | grep -v ":0$" | wc -l | grep -q "^0$" && echo "TONE_CLEAN" || echo "CHECK_TONE"</automated>
  </verify>
  <acceptance_criteria>
    - docs/integration.md exists as a new file
    - docs/integration.md frontmatter contains `layout: default`
    - docs/integration.md frontmatter contains `title: Agent Framework Integration`
    - docs/integration.md frontmatter contains `nav_order: 2`
    - docs/integration.md contains `# Agent Framework Integration` followed by `{: .fs-9 }`
    - docs/integration.md contains `{: .fs-6 .fw-300 }` after subtitle
    - docs/integration.md contains `## What is OpenClaw/ZeroClaw?` section
    - docs/integration.md contains `## Ecosystem Architecture` section with `graph TD` Mermaid block
    - docs/integration.md Mermaid diagram contains nodes: AF, SM, SK, WR, VM (matching README ecosystem diagram)
    - docs/integration.md contains `## Wiring Configuration` section with JSON config snippet containing `"mcpServers"`
    - docs/integration.md contains `## Example Workflows` section with at least 2 workflow descriptions
    - docs/integration.md contains `## Performance Characteristics` section
    - docs/integration.md contains link to home page Why Go section: `[Why Go?](/#why-go)`
    - docs/_config.yml description field is exactly: `Production-grade MCP server for Strava API -- built in Go for agent frameworks`
    - docs/_config.yml still contains `color_scheme: dark` (unchanged)
    - docs/_config.yml still contains `remote_theme: just-the-docs/just-the-docs` (unchanged)
    - docs/_config.yml still contains `search_enabled: true` (unchanged)
    - No banned words in docs/integration.md or docs/_config.yml
  </acceptance_criteria>
  <done>docs/integration.md is a complete OpenClaw/ZeroClaw integration guide with ecosystem diagram, wiring config, example workflows, and performance summary. docs/_config.yml description updated to production-grade positioning.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify docs site and update GitHub About description</name>
  <files>README.md, docs/index.md, docs/integration.md, docs/_config.yml</files>
  <action>This is a human verification checkpoint. No automated action required.</action>
  <what-built>
Complete docs site update across 3 files:
- docs/index.md: Updated hero tagline, feature bullets, Why Go? table, integration page link
- docs/integration.md: New page with ecosystem diagram, wiring config, example workflows
- docs/_config.yml: Updated description

Plus README.md (from Plan 01) with full production-grade positioning.
  </what-built>
  <how-to-verify>
1. **README rendering:** Visit https://github.com/Stealinglight/StravaMCP and verify:
   - Hero tagline says "production-grade MCP server"
   - "Why Go?" section renders with comparison table
   - "Agent Framework Integration" section renders with Mermaid ecosystem diagram
   - All 3 Mermaid diagrams render correctly (How It Works, Architecture, Ecosystem)
   - No "portfolio" or casual language visible

2. **Docs site rendering:** After push triggers deploy-docs.yml, visit https://stealinglight.github.io/StravaMCP and verify:
   - Home page tagline matches README
   - "Why Go?" table renders on home page
   - "Agent Framework Integration" appears in left navigation
   - Integration page loads with ecosystem diagram and wiring config
   - Dark theme renders correctly

3. **Manual step (D-11):** Update your GitHub repository "About" description to:
   `Production-grade MCP server for Strava API -- built in Go for agent frameworks`
   (Settings > scroll to top of repo page > click gear icon next to "About")
  </how-to-verify>
  <verify>
    <automated>cd /Volumes/DataDeuce/Projects/StravaMCP && echo "CHECKPOINT: requires human verification of rendered pages"</automated>
  </verify>
  <done>User has verified README and docs site rendering, confirmed all Mermaid diagrams display correctly, and updated GitHub About description.</done>
  <resume-signal>Type "approved" after verifying rendering and updating GitHub About, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "## Why Go?" docs/index.md` returns 1
2. `test -f docs/integration.md` succeeds
3. `grep -c "nav_order: 2" docs/integration.md` returns 1
4. `grep "Production-grade" docs/_config.yml` matches the description field
5. `grep -rciE "portfolio|showcase|demo|toy|experiment|hobby|personal project|side project" docs/index.md docs/integration.md docs/_config.yml` returns 0 for each file
6. `go test ./...` passes (no Go source accidentally modified)
7. Performance table numbers in docs/index.md match README.md exactly
8. Mermaid ecosystem diagram in docs/integration.md matches README.md exactly
</verification>

<success_criteria>
Docs site mirrors README production-grade positioning with:
- docs/index.md: matching tagline, updated features, full Why Go? table (D-08 standalone)
- docs/integration.md: new page with ecosystem explainer, Mermaid diagram, wiring config, example workflows (D-07)
- docs/_config.yml: updated description (D-10)
- User notified to update GitHub About description (D-11)
- Zero banned words across all docs files
- Integration page discoverable in site navigation via nav_order: 2
</success_criteria>

<output>
After completion, create `.planning/phases/05-openclaw-positioning-performance-messaging/05-02-SUMMARY.md`
</output>
